data:
  root: data/chest_xray
  img_size: 384
  batch_size: 32
  num_workers: 4

model:
  name: densenet121
  pretrained: true

training:
  epochs: 15
  lr: 0.0003
  weight_decay: 0.0001
  amp: true
  pos_weight: 1.5

eval:
  target_sensitivity: 0.90
  save_dir: outputs/baseline_densenet121
  seed: 42